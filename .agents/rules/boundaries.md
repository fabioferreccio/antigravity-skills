# Boundaries

1. Skills must be self-contained within their respective `.agents/skills/<name>` directories.
2. Skills should not modify files outside the workspace directory without explicit permission.
3. Secrets and API keys must never be committed.
