# Workflow

1. Create a new skill in `.agents/skills/<skill-name>`.
2. Add `SKILL.md` with correct frontmatter.
3. Add `README.md`, `tests/`, and `examples/`.
4. Run `npm run validate` to ensure the skill passes validation.
5. Create a Conventional Commit (`feat(skill-name): description`).
