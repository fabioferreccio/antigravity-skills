---
description: "{{RULE_DESCRIPTION}}"
applyTo: "{{GLOB_PATTERN}}"
---

<!-- GitHub Copilot path-specific instruction: .github/instructions/*.instructions.md
     YAML frontmatter is required with `applyTo` glob pattern.
     This rule applies only to files matching the glob. -->

{{RULE_CONTENT}}
<!-- Write the rule content in plain markdown. e.g.:

## React Component Guidelines

- Use functional components with hooks
- Define Props interface above the component
- Export components as named exports
- Place styles in co-located .module.css files
- Include displayName for debugging

## Error Boundaries

- Wrap page-level components in ErrorBoundary
- Log errors to the monitoring service
- Show user-friendly fallback UI
-->
