# {{RULE_TOPIC}}

<!-- Claude Code rule file — .claude/rules/*.md
     No YAML frontmatter. One topic per file. Keep under 250 words.
     Claude loads all files in .claude/rules/ automatically. -->

## Context

{{RULE_CONTEXT}}
<!-- When does this rule apply? e.g.:
     - "When modifying files in src/api/"
     - "When writing or reviewing tests"
     - "When handling user authentication" -->

## Requirements

{{RULE_REQUIREMENTS}}
<!-- Actionable, bulleted requirements. e.g.:
     - Always validate input parameters before processing
     - Use the project's custom error classes, never throw raw Error
     - Include JSDoc comments on all exported functions
     - Prefer async/await over .then() chains -->

## Examples

{{RULE_EXAMPLES}}
<!-- Optional: show correct and incorrect patterns.

✅ Correct:
```
// example of correct usage
```

❌ Incorrect:
```
// example of what NOT to do
```
-->
