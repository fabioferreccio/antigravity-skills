> See AGENTS.md for full project context.

# Claude Specific Context
- Refer to `.agents/rules/governance.md` for project rules.
- Run tests and validate using `npm run validate` before finalizing changes.
